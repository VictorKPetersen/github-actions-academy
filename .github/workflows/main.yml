name: Main Workflow

on:
  push:
    branches: [ main ]
  
jobs:
  Build:
    runs-on: ubuntu-latest
    container: gradle:6-jdk11
    steps:
      - name: Clone down repository
        uses: actions/checkout@v5
      - run: ci/buildapp.sh
